import { Routes } from '@angular/router';
import { VehicleInspectionComponent } from '../Components/vehicle-inspection/vehicle-inspection.component';

export const routes: Routes = [
  // { path: 'inspection', component: VehicleInspectionComponent },
  // { path: '', redirectTo: '/inspection', pathMatch: 'full' }  // Add a default route if necessary
];
